Mathula TV configurable prompts — five-call chain v3.2

Files:
  translation.system.txt / translation.user.txt
  semantic_audit.system.txt / semantic_audit.user.txt
  naturalness_audit.system.txt / naturalness_audit.user.txt
  arbitration.system.txt / arbitration.user.txt
  verification.system.txt / verification.user.txt

User prompts receive {{PAYLOAD_JSON}}. The verification user prompt also receives
{{REQUIRED_VERIFICATION_CHECKS}}.

Version 3.2 adds mandatory verification obligations generated by Python. The
verification prompt must retain obligation_results and answer every supplied
obligation_id exactly once.

v3.2.2 specialist-evidence behavior:
- Slightly inexact audit excerpts are grounded locally to exact transcript text.
- Approximate fallback grounding is labeled in the audit payload.
- The arbiter must independently verify approximate claims and may not patch from them alone.
- This prevents one malformed finding from retrying an entire specialist model call.
